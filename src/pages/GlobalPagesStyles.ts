import styled from "styled-components";

export const Container = styled("div")({
  gap: "24px",
  justifyContent: "flex-start",
  marginTop: "20px",
  display: "flex",
  flexWrap: "wrap",
});
